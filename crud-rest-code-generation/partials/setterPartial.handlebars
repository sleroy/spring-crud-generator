/**
    * Sets the {{field.name}} field
    *
    * @param {{field.name}} the {{field.name}}
    */
public void set{{capitalize field.name}}({{{javatype field.type}}} {{{field.name}}} ) {
    this.{{{field.name}}} = {{{field.name}}};
}

