/**
    * Sets the {{field.name}} field
    *
    * @param {{field.name}} the {{field.name}}
    */
public {{{javatype field.type}}} set{{capitalize field.name}}({{{javatype field.type}}} {{{field.name}}} ) {
    this.{{{field.name}}} = {{{field.name}}};
}
