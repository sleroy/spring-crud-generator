{{#*inline "fieldPartial"}}
    private {{javatype field.type}} {{field.name}};
{{/inline}}

package {{dto.packageName}};

/**
* Light DTO Entity without relationships for {link {{originalEntity.canonicalName}} }
*/
public class {{dto.simpleName}} {
{{#each dto.fields as |field|}}
    {{> fieldPartial field=field}}
{{/each}}

    /**
     * Instantiate a new DTO.
     */
    public {{dto.simpleName}}() {
    super();
    }


}

