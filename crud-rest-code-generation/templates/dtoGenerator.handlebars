package {{dtoPackageName}};

\{{> imports}}

/**
* Light DTO Entity without relationships for {link {{originalEntity.canonicalName}} }
*/
public class {{dtoName}} extends {{dtoLightName}} {
{{#each fields as |field|}}
    {{> fieldPartial field=field}}
{{/each}}

    /**
     * Instantiate a new DTO.
     */
    public {{dtoName}}() {
        super();
    }

    {{#each fields as |field|}}
    {{> getterPartial field=field}}
    {{/each}}

    {{#each fields as |field|}}
    {{> setterPartial field=field}}
    {{/each}}


}

