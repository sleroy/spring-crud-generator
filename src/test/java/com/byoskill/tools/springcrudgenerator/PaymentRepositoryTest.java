package com.byoskill.tools.springcrudgenerator;


import org.junit.Test;

import java.io.IOException;

public class PaymentRepositoryTest {

    @Test
    public void testGeneration() throws ClassNotFoundException, IOException {
        /**var                       clazz                     = PaymentRepository.class;
        RepositoryMetadataScanner repositoryMetadataScanner = new RepositoryMetadataScanner(clazz);
        repositoryMetadataScanner.scan();

        GenerationOptions    options              = new GenerationOptions();
        SaltRestApiGenerator saltRestApiGenerator = new SaltRestApiGenerator(repositoryMetadataScanner, options);
        saltRestApiGenerator.generate();
*/

    }

}